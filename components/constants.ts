export const UI_STRINGS = {
  searchPlaceholder: "Search corridors, events, zones…",
  clockLabel: "IST",
  // Add more UI strings as needed
};

export const THEME = {
  // Example static colors (replace with actual values from the codebase)
  primary: "#0ea5e9",
  background: "#ffffff",
  // Add more colors as needed
};

export const DEFAULTS = {
  // Example defaults
  paginationSize: 20,
  // Add more defaults as needed
};
